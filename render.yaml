services:
  - type: web
    name: houston-flood-dashboard
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn flood_dashboard:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
